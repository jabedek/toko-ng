/**
 * loader.service loads resources and initial settings
 * synthesis.service manages actual processes of speech synthesis
 * event-handler.service handles all events produced by other synthesis services
 */
